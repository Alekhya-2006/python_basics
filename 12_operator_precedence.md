# Operator precedence:
- priority of the operators
-       ()
        **
      *, /, %
        +, -
==, !=, >, >=, <, <=
        not
        and
        or
- If same precedence, then slove the expression from left to right        